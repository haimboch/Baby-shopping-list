# תכנית פיילוט סגור — Baby Smart List v0.56

## מטרת הפיילוט

לוודא שבשימוש יומיומי אמיתי משפחה יכולה לעדכן חוסר, לקבל Push כשהאפליקציה סגורה, לזהות את האריזה בסופר ולמצוא את המחיר המתאים באזור שלה.

## היקף מומלץ

- משך: 14 ימים.
- התחלה: המשפחה הנוכחית למשך יומיים.
- הרחבה: עד 3 משפחות, שני משתמשים בכל משפחה.
- בכל משפחה: לפחות 3 מוצרים יומיומיים עם ברקוד מדויק, כולל תמ״ל רק אם המשפחה משתמשת בו.

## הכנת כל משפחה

1. המשתמש הראשון יוצר משפחה ומגדיר עיר, מרכז ורדיוס.
2. המשתמש השני מצטרף באמצעות קוד המשפחה.
3. כל אחד מתקין את האפליקציה מהדפדפן ופותח אותה מהאייקון.
4. כל אחד מפעיל Push ומריץ בדיקת התראה.
5. בוחרים אריזה מדויקת לכל מוצר. כאשר חסרה תמונה, מצלמים את האריזה.
6. פותחים את `מדריך הפיילוט` ומוודאים שכל ששת השלבים מסומנים.

## שלושת תרחישי החובה

### 1. חוסר בין שני מכשירים

- סוגרים את האפליקציה במכשיר ב׳.
- במכשיר א׳ מסמנים מוצר `חסר`.
- מוודאים ש־Push מגיע למכשיר ב׳.
- לוחצים על ההתראה ומוודאים שנפתחת השוואת המחירים למוצר.

### 2. איתור מוצר בסופר

- פותחים את המוצר מרשימת הקניות.
- משווים את תמונת האריזה והברקוד למוצר שעל המדף.
- אם חסרה תמונה, מצלמים את חזית האריזה באפליקציה.

### 3. קנייה וחיסכון

- פותחים השוואת מחירים באזור.
- בודקים שהמחיר, המבצע והכמות תואמים להצעה.
- שומרים קנייה ומוודאים שהמלאי והחיסכון עודכנו.
- בתמ״ל מוודאים שאין הצעת החלפה למותג אחר.

## מדדי הצלחה

| מדד | יעד לפיילוט |
|---|---:|
| משפחות שסיימו הכנה | 100% |
| משתמשים עם Push פעיל | 100% |
| התראות חוסר שהגיעו כשהאפליקציה סגורה | לפחות 90% |
| לחיצות Push שנפתחו במוצר הנכון | 100% |
| מוצרים פעילים עם ברקוד מדויק | לפחות 90% |
| האריזות הפעילות עם תמונה או צילום משפחתי | לפחות 90% |
| תקלות שחוסמות קנייה או Push | 0 פתוחות לפני הרחבת הפיילוט |
| אירועי גישה בין משפחות | 0 |

## ניהול יומי למפעיל הפיילוט

ב־Supabase Table Editor בדקו פעם ביום:

- `pilot_feedback_reports` — פניות חדשות לפי קטגוריה וסטטוס;
- `data_deletion_requests` — בקשות שממתינות לטיפול;
- `pilot_events` — האם הפעולות המרכזיות מתבצעות;
- `push_delivery_logs` — הצלחות וכישלונות Push;
- `baby_product_catalog` — כיסוי תמונות האריזה.

שאילתות קריאה שימושיות:

```sql
select event_name, count(*)
from public.pilot_events
where created_at >= now() - interval '14 days'
group by event_name
order by count(*) desc;
```

```sql
select category, status, count(*)
from public.pilot_feedback_reports
group by category, status
order by status, category;
```

```sql
select request_scope, status, requested_at
from public.data_deletion_requests
where status in ('pending', 'processing')
order by requested_at;
```

אין לסמן בקשת מחיקה כ־`completed` לפני שהמחיקה עצמה בוצעה ואומתה. בגרסה 0.56 הבקשה נאספת ומנוהלת, אך המחיקה בפועל היא פעולה ידנית של מנהל המערכת.

## כללי החלטה

- תקלה ב־Push סגור, גישה למשפחה לא נכונה או פתיחת מוצר שגוי עוצרת הרחבה למשפחות נוספות.
- תמונה חסרה אינה עוצרת את הפיילוט אם יש ברקוד מדויק ואפשרות צילום, אך היא נכנסת לרשימת השלמה.
- משוב על נוחות או ניסוח נאסף במהלך הפיילוט ואינו עוצר שימוש, אלא אם הוא גורם לקנייה שגויה.
- אחרי 48 שעות נקיות במשפחה הראשונה אפשר לצרף את שתי המשפחות הנוספות.

## סיום הפיילוט

בסוף 14 הימים מסכמים:

1. כמה התראות נשלחו וכמה נפתחו;
2. כמה מוצרים זוהו עם ברקוד ותמונה;
3. כמה השוואות הובילו לרישום קנייה;
4. אילו תקלות חזרו אצל יותר ממשפחה אחת;
5. האם אפשר לפתוח שימוש רחב או שנדרשת גרסת תיקון ממוקדת.
