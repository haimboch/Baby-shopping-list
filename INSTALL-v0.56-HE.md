# התקנת Baby Smart List v0.56 ב־GitHub

המדריך מיועד להטמעה ידנית במאגר:

`https://github.com/haimboch/Baby-shopping-list`

## לפני שמתחילים

1. היכנסו ל־GitHub ובחרו בענף `main`.
2. ודאו שאין עריכה אחרת שממתינה להעלאה.
3. אין צורך להריץ SQL בפרויקט Supabase הפעיל: מיגרציית v0.56 כבר הוחלה ונבדקה.
4. אין להעלות את קובץ ה־ZIP עצמו לתוך המאגר. פותחים אותו במחשב ומעלים את הקבצים שבתוכו.

## סדר ההעלאה

### שלב 1 — קובצי האפליקציה בשורש המאגר

במסך הראשי של המאגר לחצו `Add file` ואז `Upload files`, והחליפו את הקבצים הבאים:

| קובץ | יעד במאגר | פעולה | למה הוא נדרש |
|---|---|---|---|
| `index.html` | `/index.html` | החלפה | ממשק Pilot Ready, משוב, פרטיות ומדדים |
| `service-worker.js` | `/service-worker.js` | החלפה | מעבר למטמון `baby-smart-v056-pilot-ready` |
| `self_test.py` | `/self_test.py` | החלפה | מוסיף את בדיקות v0.56 לריצת ה־Action |
| `README-v0.56.md` | `/README-v0.56.md` | חדש | תיעוד הגרסה |
| `INSTALL-v0.56-HE.md` | `/INSTALL-v0.56-HE.md` | חדש | מדריך זה |
| `PILOT-v0.56-HE.md` | `/PILOT-v0.56-HE.md` | חדש | תכנית הפיילוט |

הודעת Commit מומלצת:

`Release v0.56 Pilot Ready`

### שלב 2 — קובץ המיגרציה לתיעוד המאגר

פתחו במאגר את התיקייה `supabase/migrations`, לחצו `Add file` ואז `Upload files`, והעלו:

`20260907235608_v056_pilot_ready.sql`

הקובץ נדרש כדי שהיסטוריית הקוד תהיה זהה למסד הפעיל. **אין להריץ אותו שוב** בפרויקט Supabase הפעיל.

### שלב 3 — קובצי הבדיקות

פתחו את התיקייה `tests` והעלו את הקבצים הבאים:

| קובץ | פעולה |
|---|---|
| `test_v056_pilot_ready.py` | חדש |
| `test_v055_push_diagnostics.py` | החלפה |
| `test_v053_release_contract.py` | החלפה |
| `test_v050_authorized_superpharm_bulk.py` | החלפה |
| `test_v048_uiux_contract.py` | החלפה |

השינויים בקובצי הבדיקות הישנים אינם משנים את האפליקציה; הם רק מסירים בדיקות שננעלו על שם של גרסה ישנה.

### שלב 4 — קבצים המצורפים רק כעותק ביטחון

`manifest.webmanifest`, `app-icon-192.png` ו־`app-icon-512.png` לא השתנו. אין צורך להחליף אותם אם הם כבר קיימים במאגר.

גם `.github/workflows/collect-product-images.yml` כבר קיים במאגר ולא השתנה. הוא מצורף לחבילה לצורך בדיקה בלבד.

## הפעלת איסוף התמונות מיד אחרי ההעלאה

1. היכנסו ללשונית `Actions` במאגר.
2. בחרו `Collect verified product package images`.
3. לחצו `Run workflow`, בחרו `main`, ואשרו.
4. המתינו לסימון ירוק בשני השלבים `Validate image collector` ו־`Collect exact-barcode package images`.
5. אם הריצה נכשלת בשלב הסודות, בדקו ב־`Settings` → `Secrets and variables` → `Actions` שקיימים:
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `CHEAPERSAL_API_KEY` — אופציונלי, אך משפר מקורות חיפוש מסוימים.

לא מציגים או מעתיקים את ערכי הסודות לתוך קובץ, Issue או צילום מסך.

## רענון גרסה במכשירים

בכל אחד משני המכשירים:

1. סגרו לגמרי את האפליקציה המותקנת.
2. פתחו פעם אחת את כתובת האתר בדפדפן ורעננו.
3. סגרו ופתחו מחדש מהאייקון במסך הבית.
4. בתחתית המסך צריך להופיע `Pilot Ready v0.56`.
5. אם עדיין מופיעה גרסה ישנה, סגרו שוב, המתינו כחצי דקה ופתחו מחדש. רק אם זה לא עוזר, הסירו את האייקון והתקינו שוב.

## בדיקות חובה לאחר ההטמעה

בצעו לפי הסדר:

| # | בדיקה | תוצאה נדרשת |
|---:|---|---|
| 1 | כניסה עם משתמש קיים | שם המשתמש אינו מתבקש שוב |
| 2 | פתיחת הבית | מופיע `Pilot Ready v0.56` ומסך מוכנות |
| 3 | לחיצה על `עזרה` | נפתחים משוב, מדריך פיילוט ופרטיות |
| 4 | מדריך הפיילוט | מוצגים 2 משתמשים ו־2 חיבורי Push |
| 5 | בדיקת Push בכל מכשיר | מתקבלת התראת בדיקה אמיתית |
| 6 | מכשיר א׳ מסמן מוצר `חסר`, כאשר האפליקציה סגורה במכשיר ב׳ | מכשיר ב׳ מקבל Push |
| 7 | לחיצה על ה־Push | נפתחת השוואת המחירים של המוצר המדויק |
| 8 | מוצר שאינו תמ״ל | חלופה זולה באזור מוצגת רק כאשר היא באמת זולה |
| 9 | מוצר תמ״ל | אין המלצה להחליף למוצר אחר |
| 10 | מוצר עם ברקוד | הברקוד ותמונת האריזה המתאימה מוצגים; אם חסרה תמונה מופיע כפתור צילום |
| 11 | שליחת דיווח דרך `עזרה` | מתקבלת הודעה שהפנייה נשמרה |
| 12 | פרטיות → בקשת מחיקה אישית → ביטול | הבקשה מופיעה ואז נעלמת מרשימת הבקשות הפתוחות |
| 13 | יצירת סל וקנייה | הסל, המחיר, הכמות והחיסכון נשמרים |

בבדיקה 12 השתמשו בבקשה אישית ובטלו אותה מיד. אין לבדוק מחיקת משפחה בסביבת הנתונים הפעילה.

## אם אחת הבדיקות נכשלת

- ודאו שהקבצים הועלו לענף `main` ובנתיב הנכון.
- בדקו שה־Commit מופיע בראש המאגר.
- פתחו `Actions` ובדקו שאין ריצה אדומה.
- במכשיר הבעייתי פתחו `התראות`, לחצו שוב על הפעלה והריצו בדיקת Push.
- שלחו דיווח מתוך `עזרה` וציינו באיזו בדיקה נכשלתם ובאיזה מכשיר.

אין לבצע שינוי SQL נוסף או לפרוס מחדש את פונקציית ה־Push עבור גרסה זו.
